# Terraform state and sensitive files
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Plan binary — never commit, contains sensitive resource values
tfplan

# IDE / tool-specific
.claude/

# OS
.DS_Store
Thumbs.db
